import { AgregarMetricasComponent } from './agregar-metricas.component';

describe('AgregarMetricasComponent', () => {
  let component: AgregarMetricasComponent;

  beforeEach(() => {
    component = new AgregarMetricasComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
