import { LayoutMetricasComponent } from './layout-metricas.component';

describe('LayoutMetricasComponent', () => {
  let component: LayoutMetricasComponent;

  beforeEach(() => {
    component = new LayoutMetricasComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
