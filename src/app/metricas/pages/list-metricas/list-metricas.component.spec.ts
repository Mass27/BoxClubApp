import { ListMetricasComponent } from './list-metricas.component';

describe('ListMetricasComponent', () => {
  let component: ListMetricasComponent;

  beforeEach(() => {
    component = new ListMetricasComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
